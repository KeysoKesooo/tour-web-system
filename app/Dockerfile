# Dockerfile (for documentation, not for running)
# ------------------------------------------------
# FROM node:20-alpine
# WORKDIR /app
# COPY package.json package-lock.json ./
# RUN npm install
# COPY . .
# EXPOSE 3000
# CMD ["npm", "run", "dev"]
# ------------------------------------------------
# This Dockerfile is for reference/documentation only.
# We are running Next.js locally, not inside Docker.
